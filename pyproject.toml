[project]
name = "troop"
version = "0.0.1"
description = "A Python package"
authors = []
dependencies = []
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
